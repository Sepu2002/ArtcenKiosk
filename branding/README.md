Drop this site's client logo here (e.g. `logo.png`), then set in `.env`:

```
BRAND_NAME=Nombre del cliente
BRAND_LOGO=branding/logo.png
BRAND_COLOR=#2563eb
BRAND_FOOTER=soporte@cliente.com
```

Leave any of these blank to fall back to the default look (no logo header,
default blue, no footer).
